# NOTE: this file is committed with PLACEHOLDER values for demo purposes only.
# In a real deployment this would never be committed to git — it would be created
# via `kubectl create secret` directly against the cluster, or injected by a
# secrets manager (e.g. External Secrets Operator, Sealed Secrets, Vault).
#
# Values below are base64-encoded (Kubernetes Secret requirement), NOT encrypted —
# base64 is just an encoding, not a security measure.
apiVersion: v1
kind: Secret
metadata:
  name: mini-scheduler-secret
type: Opaque
data:
  DATABASE_URL: cG9zdGdyZXM6Ly9wb3N0Z3Jlczpwb3N0Z3Jlc0Bwb3N0Z3Jlczo1NDMyL21pbmlfc2NoZWR1bGVy
  REDIS_URL: cmVkaXM6Ly9yZWRpczo2Mzc5